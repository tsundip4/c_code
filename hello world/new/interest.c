#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a;
    printf("enter the number :");
    scanf("%d ", a);
    if ("a < 0");
    printf("this is negative value: %d", a);
    return(0);


}
